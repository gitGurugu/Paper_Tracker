# Core
arxiv>=2.1.0
pydantic>=2.0.0
jinja2>=3.1.0
python-dotenv>=1.0.0
click>=8.1.0
httpx>=0.27.0

# LLM provider SDK
# Only the selected provider's SDK is imported at runtime (lazy-loaded in
# get_analyzer, src/llm/__init__.py). The default install covers the
# OpenAI-compatible provider, which serves both OpenAI and third-party /
# proxy endpoints (DeepSeek, Kimi, OpenRouter, etc.).
openai>=1.0.0              # provider: openai

# Optional providers — uncomment the one you set in data/config.json:
# anthropic>=0.40.0         # provider: claude
# ollama>=0.4.0             # provider: ollama (local models)
# google-generativeai>=0.8.0  # provider: gemini
